org.gradle.jvmargs=-Xmx2048m
# play-services-location depends on AndroidX, so this has to be on. The app's
# own code still uses android.app.Activity and no support library.
android.useAndroidX=true
android.nonTransitiveRClass=true
